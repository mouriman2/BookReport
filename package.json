{
  "name": "BookReport",
  "version": "1.0.0",
  "private": true,
  "description": "PROJECT_DESCRIPTION",
  "main": "index.js",
  "scripts": {
    "init": "gitbook init",
    "start": "gitbook serve  --open",
    "build": "gitbook build",
    "publish": "gh-pages -d _book"
  },
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "gitbook-cli": "^2.3.0"
  }
}
